%table
  - @results.each do |result|
    %tr
      - @fields.each do |field|
        %th
          = (field.title || field.name)
    %tr
      - @fields.each do |field|
        %td
          = result[field.name.to_sym]